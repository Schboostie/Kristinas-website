document.addEventListener('DOMContentLoaded', function() {
  var gallery = new Flickity('.gallery', {
    // Flickity options here
    // You can customize the transition, autoplay, etc.
    // Check Flickity documentation for available options
    autoPlay: true,
    wrapAround: true
  });
});
